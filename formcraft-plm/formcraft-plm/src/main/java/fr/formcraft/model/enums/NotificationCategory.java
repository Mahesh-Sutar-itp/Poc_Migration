package fr.formcraft.model.enums;

/** Classification of a notification, used for icon/filter selection in the UI. */
public enum NotificationCategory {
    TASK,
    WORKFLOW,
    QUALITY,
    CHANGE_REQUEST,
    INVENTORY,
    SYSTEM
}
