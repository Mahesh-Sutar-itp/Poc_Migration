package fr.formcraft.model.enums;

/** The action an {@link fr.formcraft.model.entity.EventActionRule} dispatches when its event fires. */
public enum EventActionType {
    NOTIFY_ROLE,
    NOTIFY_INITIATING_USER
}
